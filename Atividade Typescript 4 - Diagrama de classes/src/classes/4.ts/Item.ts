export abstract class Item {
    private _titulo: string;
    private _disponivel: boolean;

    constructor(titulo: string, disponivel: boolean) {
        this._titulo = titulo;
        this._disponivel = disponivel = true;
    }

    
    public get getDisponivel() : string {
        return 
    }
    
}